/**
 * GSEP CLI - Import Command
 */

export async function importGenome(_file: string, _options: Record<string, unknown>): Promise<void> {
    console.error('Error: The "import" command is not yet available.');
    console.error('Track progress: https://github.com/gsepcore/pga-platform/issues');
    process.exit(1);
}
